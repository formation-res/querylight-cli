# Querylight CLI

Querylight CLI builds lightweight local knowledge bases from files, websites, and URLs.

## Features

It supports ingestion, chunking, indexing, search, context generation, and answer prompting.
